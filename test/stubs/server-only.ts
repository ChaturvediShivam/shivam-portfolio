// Test stub for the `server-only` package. In the Next.js server build this
// module is a no-op; in a plain Node (vitest) run there is no `react-server`
// condition, so the real package would throw on import. Aliasing to this empty
// module lets us unit-test server-only logic modules directly.
export {};
